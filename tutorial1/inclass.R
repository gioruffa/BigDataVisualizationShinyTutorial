library(shiny)
runExample("01_hello")

#two mandatory files for shiny
#ui.R -> user interface widgets with callbacks
#server.R -> where the callbacks are implemented
#you can create them with new->Shiny Web App